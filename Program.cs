namespace SnakeAndLadder
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Console.WriteLine("Hello, World!");
            // SnakeAndLadderProblem.Call();
            //RollDice.call();
            // RolldiceLadderGame.call();
            //SnakeAndLadderRolldice.call();
            //WinningPosition.call();
           // RollCount.call();
            GameWith2Player.call();

        }
    }
}